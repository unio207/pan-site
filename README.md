# Pan — Marketing Site

Waitlist + linktree page for [Pan](pan-site-psi.vercel.app).

Built with React + Fraunces/DM Sans. Deployed on Vercel.

## Deploying

This is a single static HTML file (`index.html`). Connect the repo to Vercel and it deploys automatically on every push.
